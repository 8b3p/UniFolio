<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
  <link rel="stylesheet" href="css/home.css">
  <title>Home</title>
</head>

<body>
  <div class="wrapper">
    <div class="container-fluid h-75 d-flex flex-column justify-content-between">
      <div class="container">
        <nav class="navbar  navbar-dark bg-transparent">
          <a class="navbar-brand" href="#">Finance</a>
          <ul class="navbar-nav d-flex flex-row justify-content-between">
            <li class="nav-item">
              <a class="nav-link mx-2" href="/logout">تسجيل الخروج</a>
            </li>
          </ul>
        </nav>
      </div>
      <div class="container d-flex justify-content-center align-items-center my-auto">
        <main class="card">
          <h1 class="text-end">
            <%= ((balance * (currentUser.percentage / 100)) - (((balance * (currentUser.percentage / 100)) -
              currentUser.capital) * ((100 - currentUser.profit) / 100))).toFixed(2) %>$ : الرصيد الكلي
          </h1>
          <% if (currentUser.username=='omair' ) { %>
            <h3 class="text-center"><%= balance %> : جميع الحسابات
            </h3>
            <% } %>
              <p class="text-end">
                <%= currentUser.capital %>$ : رأس المال
              </p>
              <p class="text-end">
                <%= (((balance * (currentUser.percentage / 100)) - currentUser.capital) * (currentUser.profit /
                  100)).toFixed(2) %>$ (
                  <%= (((((balance * (currentUser.percentage / 100)) - currentUser.capital) * (currentUser.profit /
                    100)) * 100) / (currentUser.capital)).toFixed(2) %>%) : الربح
              </p>
              <% if(commissioned) { %>
                <p><%=((((balance * (commissioned.percentage / 100)) - commissioned.capital) * ((100
                    - commissioned.profit) / 100))).toFixed(2) %>$ : ربح العمولة
                </p>
                <p><%= ((((balance * (currentUser.percentage / 100)) - currentUser.capital) *
                    (currentUser.profit / 100)) + ((((balance * (commissioned.percentage / 100)) - commissioned.capital)
                    * ((100 - commissioned.profit) / 100)))).toFixed(2) %>$ : اجمالي الربح</p>
                <% } %>
        </main>
      </div>
    </div>
    <div class="spacer layer1"></div>
    <div class="darayya container-fluid h-100 d-flex flex-column justify-content-between">
      <div class="container d-flex justify-content-center align-items-center my-auto">
        <section class="card">
          <h1>this is for additional information</h1>
        </section>
      </div>
      <div class="spacer layer2"></div>
    </div>
    <footer class="footer bg-transparent py-3">
      <div class="container text-center">
        <span class="text-muted">&copy: Finance, Omair</span>
      </div>
    </footer>

  </div>
  <script src="https://code.jquery.com/jquery-3.2.1.slim.min.js"
    integrity="sha384-KJ3o2DKtIkvYIK3UENzmM7KCkRr/rE9/Qpg6aAZGJwFDMVNA/GpGFF93hXpG5KkN"
    crossorigin="anonymous"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.12.9/umd/popper.min.js"
    integrity="sha384-ApNbgh9B+Y1QKtv3Rn7W3mgPxhU9K/ScQsAP7hUibX39j7fakFPskvXusvfa0b4Q"
    crossorigin="anonymous"></script>
  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/js/bootstrap.min.js"
    integrity="sha384-JZR6Spejh4U02d8jOt6vLEHfe/JQGiRRSQQxSfFWpi1MquVdAyjUar5+76PVCmYl"
    crossorigin="anonymous"></script>
</body>

</html>